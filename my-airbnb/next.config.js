/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images : {
    domains : ['virtserver.swaggerhub.com']
  }
}


module.exports = {
  nextConfig,
  images : {
    domains : ['virtserver.swaggerhub.com']
  }
  
}
